//Ensembles des fonctions qui gère la commande exit
#ifndef PWD_H
#define PWD_H

//Ajout includes si necessaire...

int pwd(char* args);

int pwdForP();

int pwdForL();

char* printError(char* error_msg);

#endif